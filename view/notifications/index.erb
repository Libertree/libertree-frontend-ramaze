<h2><%=_('Notifications')%></h2>

<div style="margin-bottom: 1.5em;">
  <a id="mark-all-notifications-seen" href="#"><%=_('mark all seen')%></a>
</div>

<% if @set_keys.empty? %>
    <%=_('You have no new notifications.')%>
<% else %><%= render_partial '_notifications', sets: @sets, set_keys: @set_keys %><% end %>
